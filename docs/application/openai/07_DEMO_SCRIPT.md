# 公開 Demo の進行

1. `README.md` を開き、OpenAI endorsement、完成製品 release、owner GO を主張していないことを示す。
2. `apps/desktop_flutter` を操作者向け UI として示す。
3. `native/rust_helper` を broker / native helper 境界として示す。
4. `specs` と `tooling/conformance_tests` を契約 gate として示す。
5. `python3 tooling/validate_all.py --python-only` を実行する。
6. `public_assets/windows_proof_pack/00_WINDOWS_PROOF_INDEX.md` を示す。
7. 公開 proof asset は墨消し済み review copy であり、canonical release evidence ではないと説明する。

## 外部公開用英語射影（非正本）

```text
Show the non-claims, UI, Rust boundary, schemas/conformance, local validation, and sanitized proof index. End by stating that public proof copies do not replace canonical evidence or owner GO.
```
