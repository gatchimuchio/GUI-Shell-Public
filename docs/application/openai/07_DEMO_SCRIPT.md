# Demo Script

1. Open `README.md` and state the non-claims: no OpenAI endorsement, no completed product release claim, owner GO remains a release gate.
2. Show `apps/desktop_flutter` as the operator UI.
3. Show `native/rust_helper` as the broker/native helper boundary.
4. Show `specs` and `tooling/conformance_tests` as the contract gate.
5. Run `python3 tooling/validate_all.py --python-only`.
6. Show `public_assets/windows_proof_pack/00_WINDOWS_PROOF_INDEX.md`.
7. Explain that public proof assets are sanitized review material, while strict release status remains governed separately.
