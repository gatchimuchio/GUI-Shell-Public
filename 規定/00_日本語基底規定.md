# GUI-Shell-Public 日本語基底規定

> 状態: 現行局所正本
> 版: `2026-09-03-日本語基底-1-public`
> 規定言語・基底言語: 日本語
> 適用範囲: GUI-Shell-Public が所有する公開文書、意味、設計、契約説明、実装上の独自概念、監査、検証、運用報告、公開・非公開境界

## 0. 位置づけ

本規定は、GUI-Shell-Public における日本語基底の局所正本である。

GUI-Shell-Public では、対象、差異、関係、状態、作用、境界、権限、採否、失敗、復旧、検証結果、公開可否を、最初に日本語で成立させる。

本リポジトリはGUI-Shellの単純mirrorではなく、公開・非公開境界を通過した公開投影である。投影元の後続変更を自動継承せず、固定commitとの差、公開可否、意味差、公開版固有資産を監査してから採用する。

```text
日本語で対象化・差異化・関係化
→ 日本語で定義・設計・構造化
→ 日本語で実装意味・監査・検証を記述
→ 日本語正本成立
→ 実務上やむを得ない外部接続だけ他言語へ局所射影
```

外国語を第二基底、並列正本、内部意味の主語として扱わない。

本規定は次と責任を分ける。

- `AGENTS.md`: 作業規律、優先順位、安全・権限・Git・検証・完了報告の正本
- `docs/agents/PUBLIC_REPO_BOUNDARY.md`: 公開可能範囲、非公開範囲、墨消し、公開証拠の非正規性の正本
- `規定/正本索引.json`: 現行正本、責任地図、外部参照固定点の機械可読索引
- `docs/LANGUAGE_POLICY.md`: Flutter / Dart、Rust、TypeScript / Node、Python 等の実装言語・実行責任の分担方針

自然言語の日本語基底と、プログラミング言語の採用方針を混同しない。

## 1. 上位参照と局所射影

### 1.1 最上位理論参照

- Repository: `https://github.com/gatchimuchio/cognitive-engineering-foundations`
- 参照commit: `60131da52ba7931ed7f82c7648a74ac790f50d08`
- 参照対象: `PUBLICATION_POLICY.md`、`00_認知工学とは何か_v0_3_ja_en.md`、`01_情報工学における言語基底論_v0_3_ja_en.md`

GUI-Shell は、次の原則を局所射影する。

- 日本語原典を先に成立させる。
- 現行版では意味を明示的に固定し、無言変更しない。
- 現在条件で強く成立する判断は強く採用する。
- 局所判断を世界全体・将来全体の最終閉包へ昇格しない。
- 新しい実装結果・観測・反例が成立条件を崩す場合は、必要な前提まで再開放する。
- 旧版を遡及改変せず、変更理由と影響を追跡可能にする。
- 規定自身も監査対象から除外しない。

上位理論本文をGUI-Shellへ複製せず、GUI-Shell が所有する対象と責任に必要な規則だけを本規定へ記述する。

### 1.2 責任別外部参照

- Repository: `https://github.com/gatchimuchio/LLM-Constitutive-Specification`
- 現行main参照commit: `3f5eb7b704dba5a06c717399c3400405b5e8944e`
- 現行版: `2026-08-29-成立規定-9`
- GUI-Shell への参照射程: `規定/04_運用境界.md` における「言語模型物」「言語模型実行系」「利用系」の責任分離、および観測事実を上位能力へ無言昇格させない境界

GUI-Shell は主として利用系・操作制御面を担う。GUI表示、対話、道具接続、外部記憶、Adapter、監査表示の成立を、言語模型の成立、模型能力、意思決定構造、能力主体の成立へ読み替えない。

この外部規定の言語模型成立条件をGUI-Shellの製品成立条件として丸ごと取り込まない。

### 1.3 MINIDORA参照との差

- Repository: `https://github.com/gatchimuchio/NOTNN-LLM-MINIDORA`
- 調査時main参照commit: `010a4b63f23027a1ec192b55cdad0e97aa85325c`
- 同commitの局所参照: LLM成立規定v8、commit `fcbc2fa4bc89d749942e8ebee2764115488d29c4`

MINIDORA の局所参照v8と、LLM成立規定の現行main v9を同一版として扱わない。GUI-Shell はv9の限定された運用境界だけを明示参照し、MINIDORAからは日本語基底の局所射影方法、権威順序、意味監査、外部例外の局所化という運用パターンだけを参照する。

MINIDORA固有の模型構造、Compiler、評価問題、能力作用構成、HDS運用をGUI-Shellへ移植しない。

HDSについて、非公開の内部構造、相、層、循環、判断機構、評価設計、運用手順、実装、再現可能な手順を本リポジトリへ記述しない。

### 1.4 GUI-Shell投影元との差

- 投影元Repository: `https://github.com/gatchimuchio/GUI-Shell`
- 今回の投影元固定commit: `306caff92c590364b5049e7d9c27d6a14474be55`
- GUI-Shell-Public成立前基準commit: `ef706145b92fcf9ddbf1c7c06649157d60442a6b`

投影元から採用できるのは、公開境界を通過した実装、契約、検査、公開文書である。投影元に存在すること、Git追跡済みであること、日本語化済みであることだけでは公開を許可しない。

次は投影対象外とする。

- rawまたは未墨消しのrelease evidence、log、transcript、screenshot、environment dump
- 実利用者名、host名、端末名、home path、credential、secret、owner-private decision log
- `release_evidence/`と投影元の`openai_submission_assets/windows_proof_pack/`
- owner GOの非公開判断経路

`public_assets/windows_proof_pack/`の墨消し済み資産は、公開review用に保存した非正規の履歴資産である。正規release evidence、現行実行結果、`release_ready`、owner GO、release blocker解消の根拠にしない。

## 2. 日本語を正本とする対象

GUI-Shell-Public が独自に所有し、外部契約に表記を拘束されない次の対象は、原則として日本語で保持する。

- 規定、仕様、設計、責任、境界、前提、成功条件、失敗条件
- 操作者向け文書、導入文書、運用文書、復旧文書、監査報告
- 独自の対象名、状態名、作用名、関係名、権限名、採否名、失敗分類
- 契約の意味説明、Schemaの責任説明、ProtocolのGUI-Shell側意味
- 監査語彙、検証項目、試験名、fixtureの意図、失敗理由
- ログの意味区分、診断説明、利用者へ表示するメッセージ
- 実装上の独自コメント、docstring、内部概念名
- GUI-Shell が新設する独自の構造化データ鍵・区分値・識別子
- 変更理由、証拠範囲、未検証範囲、残存リスク、再開放条件

既存の公開契約、互換識別子、保存形式を日本語化する場合は、表記変更自体を目的にせず、互換性、移行、復旧、監査、利用者影響を先に閉じる。安全な移行契約なしに既存識別子を一括改名しない。

## 3. 他言語を認める局所例外

次は、正確性、相互運用性、実行可能性、検索再現性、既存互換性のため必要な範囲に限り、原形を保持できる。

1. プログラミング言語の予約語、標準構文、演算子、型構文
2. 外部API、外部Schema、通信規約、Protocolが要求する固定フィールド・固定値
3. 既存ライブラリ、framework、package、crate、module、class、function、typeの正式識別子
4. CLI の command、option、environment variable、filesystem path、branch、tag、ref
5. URL、commit hash、署名、digest、版識別子、言語コード
6. JSON Schema、IPC、HTTP、Git、SHA-256等の規格・形式・Protocol名
7. GUI Shell、Runtime Operation Shell、Shell Core、BLUE-TANUKI、Flutter、Dart、Rust等の固有名・確立用語
8. Capability、Permission、Approval、AuditEvent、RecoveryAction等の既存公開契約識別子
9. `release_blocker`、`post_v1_scope`、`known_limitation`等の機械判定値
10. 外部模型、製品、組織、論文、方式、規格の固有名
11. 原文引用、由来保存、検索、国際公開、外部連携に必要な外国語表層
12. 自動生成物、vendor由来物、lockfile、binary等、GUI-Shellが本文言語を所有しない対象
13. 墨消し済み公開証拠のうち、履歴同一性を維持するためhash固定した局所保存物

例外は、必要な接続面とその近傍だけに閉じる。英語で書きやすい、業界で一般的、既存文書が英語という理由だけで、GUI-Shell独自の意味正本を英語にしない。

例外表記が必要でも、その役割、境界、採否、失敗時挙動、監査結果は日本語で説明する。

## 4. 外部原語と確立用語の扱い

外部原語を観測した場合、原語の一般語義からGUI-Shellの内部意味を逆定義しない。

```text
外部原語・確立用語
→ 対象・作用・関係・状態・境界へ分別
→ GUI-Shellで必要な日本語意味を定義
→ 原語は外部対照名または機械識別子として保持
```

例:

```text
runtime
→ 実行対象 / 実行系 / 実行中状態 / 製品実行経路を文脈ごとに分別

state
→ 模型状態 / 作業状態 / UI局所状態 / 外部再入力状態 / 永続状態を分別

approval
→ 承認要求 / 承認資格 / 承認状態 / 承認証跡を分別
```

日本語でどの対象・差異・関係を指すか確定できない場合は、都合よく補完せず、未確定としてSUSPENDまたは調査対象にする。

## 5. コード・Schema・Protocol

コードも日本語基底の適用外ではない。ただし、実行可能性と既存契約を壊してはならない。

- GUI-Shell独自の新規概念は、実装環境が安全に許す場合、日本語識別子を優先する。
- 外部API、言語慣行、framework契約、公開Schemaとの接続識別子は原形を保持する。
- 既存の英語識別子を維持する場合、日本語の責任説明から参照できる状態を保つ。
- comment、docstring、試験意図、失敗説明、運用ログは、外部制約がない限り日本語を正本とする。
- Schemaの英語field名は、その一般語義だけで権限・信頼・承認意味を決めず、日本語仕様へ接続する参照札として扱う。
- 機械値の改名は、互換移行、旧版読取、rollback、negative test、監査証跡を伴う契約変更として扱う。

## 6. 既存英語資産の移行

本規定の成立だけで、既存リポジトリ全体の日本語基底移行が完了したとは扱わない。

既存英語資産は、次の順に境界を固定して移行する。

1. 正本、現行仕様、運用指示、利用者向け文書、履歴、生成物、外部由来物を区分する。
2. 表記ではなく、対象・責任・差異・関係・禁止・失敗条件を読み取る。
3. 日本語で意味正本を成立させる。
4. 外部識別子と日本語意味の対応を保持する。
5. 既存契約・link・test・tooling・runtimeへの影響を検証する。
6. 旧版・旧表現を無言削除せず、必要な履歴と移行根拠を残す。

公開版固有の英語資産を移行するときは、投影元の同名fileで無条件に置換しない。公開版固有の説明、応募資料、公開境界、墨消し済み資産との関係を日本語で再構成し、link、hash、claim boundaryを維持する。

履歴同一性のため凍結した墨消し済み証拠は翻訳しない。代わりに日本語の索引・説明から参照し、原文が非正規の公開review資産であることを明示する。

機械翻訳、一括置換、file名だけの改名、英単語削減率を移行完了の証拠にしない。

## 7. 意味監査

日本語基底監査は、単なる日本語文字率を評価しない。少なくとも次を確認する。

- GUI-Shell独自概念の主語が日本語で成立しているか。
- 対象、状態、作用、関係、権限、境界、採否、失敗が日本語で区別されているか。
- 外部原語の一般語義が内部分類を支配していないか。
- 日本語で保持できる差を外来語一語へ圧縮していないか。
- 多言語例外に実務上の必要性があり、局所境界に閉じているか。
- 外部識別子を削除しても、日本語説明だけで責任と安全境界を再構成できるか。
- 日本語正本と外部表層が衝突したとき、日本語正本へ戻って再監査できるか。
- `規定/正本索引.json` の責任地図、参照commit、現行正本が実ファイルと整合するか。
- language migrationが権限、Approval、AuditEvent、RecoveryAction、Content Exposure Boundaryを弱めていないか。
- 文書の日本語化をruntime、contract、releaseの完成証拠へ読み替えていないか。
- 投影元だけにある非公開情報、raw evidence、local path、identity、secretが公開版へ混入していないか。
- 墨消し済み公開資産を正規release evidenceまたは現行validationへ昇格していないか。
- `release_ready=false`とowner GO未記録の公開版不変条件を維持しているか。

翻訳または外部接続で未定義、意味衝突、関係欠落が見つかった場合、外国語側で補完せず、日本語正本を先に再監査する。

## 8. 禁止事項

- 英語名を先に作り、日本語訳を後から正本と称する。
- 日本語と英語を独立した並列正本にする。
- 外部APIやframeworkの語彙粒度をGUI-Shellの内部概念へそのまま写す。
- 外部方式名・製品名を構成要件へ無言昇格する。
- 翻訳しやすさのために日本語で保持できる差異・関係・未確定性を削る。
- 日本語化を理由に、Schema、Protocol、保存形式、公開API、CLI互換性を無計画に破壊する。
- 上位理論、LLM成立規定、MINIDORAの本文や局所実装をGUI-Shellの正本として丸写しする。
- LLM、外部metadata、memory、cache、GUI state、translation resultから権限を生成する。
- HDSの非公開内部を記述、類推、転記、再構成する。
- MINIDORAのv8参照とLLM成立規定のv9を同一版として黙って統合する。
- 外部参照のmain更新をGUI-Shellへ自動適用する。
- 投影元GUI-Shellのmain更新または追跡fileを、公開可否監査なしにGUI-Shell-Publicへcopyする。
- raw evidence、未墨消しlog、実機固有path、identity、secret、owner-private decision logを公開する。
- 墨消し済み`public_assets`を正規release evidence、現行実行結果、owner GO、`release_ready`の根拠へ昇格する。
- 公開版で`release_ready`をtrueへ変更し、またはowner GOを記録する。
- 日本語文書の存在や監査scriptのPASSだけで製品完成・release readinessを主張する。

## 9. 版更新と再開放

本規定の意味、対象、例外、監査条件を変える場合は版を更新し、少なくとも次を記録する。

- 何が変わったか。
- なぜ変わったか。
- どの既存文書・実装・契約・試験へ影響するか。
- 何を維持するか。
- 何を再開放したか。
- rollback可能な旧版はどれか。

外部参照commitまたは投影元固定commitを更新するときは、旧版との差、GUI-Shell-Publicへの適用射程、公開可否、採用事項、不採用事項を監査する。最新版であることだけを採用理由にしない。

本規定自身も監査対象である。日本語基底がGUI-Shell-Publicの意味構造保持、安全性、公開境界、操作明瞭性、相互運用性を損なう具体的観測が得られた場合は、言語だけでなく、対象、目的、損失、評価方法、例外境界まで再開放する。
